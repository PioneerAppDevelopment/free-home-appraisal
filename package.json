{
  "name": "fha-backend",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "description": "Backend (Node JS) for FHA App",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Constantine Chutis",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.22.0"
  },
  "mdSeed": {
    "seedersFolder": "./seeders"
  }
}
